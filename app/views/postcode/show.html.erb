<h1>hello world</h1>
<%= form_with(url: postcode_lookup_path, local: true) do |p| %>
  <%= label_tag(:postcode_lookup, "Postcode") %>
  <%= text_field(:postcode, :lookup) %>
  <%= p.submit "Find address" %>
  <div id="results">
<% end %>