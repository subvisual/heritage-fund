<%=
  render partial: "partials/page_title",
         locals: {
             model_object: @pre_application.pa_expression_of_interest,
      page_title: t('feasibility_or_options_work.page_title')
         }
%>

<%= render partial: "partials/summary_errors", locals: {
    form_object: @pre_application.pa_expression_of_interest,
    first_form_element: :pa_expression_of_interest_feasibility_or_options_work
} if @pre_application.pa_expression_of_interest.errors.any? %>

<%=
  form_for @pre_application.pa_expression_of_interest,
  url: :pre_application_expression_of_interest_feasibility_or_options_work,
  method: :put do |f| 
%>

  <div class="govuk-form-group <%= "govuk-form-group--error" if
    @pre_application.pa_expression_of_interest.errors[:feasibility_or_options_work].any? %>">

    <h1 class="govuk-label-wrapper">

      <%= 
        f.label :feasibility_or_options_work,
        t('feasibility_or_options_work.page_heading'),
        class: "govuk-label govuk-label--xl govuk-!-margin-bottom-4"
      %>

    </h1>

    <%=
      render partial: "partials/form_input_errors",
               locals: {form_object: @pre_application.pa_expression_of_interest,
                        input_field_id: :feasibility_or_options_work} if 
                          @pre_application.pa_expression_of_interest.errors[:feasibility_or_options_work].any?
    %>

    <%=
      f.text_area :feasibility_or_options_work,
                  rows: 10,
                  class: "govuk-textarea " \
                          "#{'govuk-textarea--error' if
                      @pre_application.pa_expression_of_interest.errors[:feasibility_or_options_work].any?}"
    %>

  </div>

  <%= 
    render(
      ButtonComponent.new(
        element: "button"
      )
    ) 
  %>

<% end %>
