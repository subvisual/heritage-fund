<%= form_tag(
    url: :pre_application_start,
    method: :put,
    local: true
  ) do |f| 
%>

  <%= render(
      ButtonComponent.new(
        element: "button",
        is_start_button: true,
        text: t("buttons.labels.start_now")
      )
    ) 
  %>

<% end %>